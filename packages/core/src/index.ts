// Public entry point for @cadcode/core: re-exports the builder API and types.
export { createBuilder } from "./builder";
export type { Builder, Handle, Body } from "./builder";
export type { EdgeQuery, FaceRef, FaceLocator } from "@cadcode/protocol";
